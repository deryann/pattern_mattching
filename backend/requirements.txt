# Dependencies are managed via pyproject.toml / uv.
# Run `uv sync` from the project root instead.
# For compatibility with non-uv workflows:
#   pip install -r backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
opencv-python>=4.8
numpy>=1.26
pydantic>=2.5
python-multipart>=0.0.9
pytest>=8.0
httpx>=0.27
